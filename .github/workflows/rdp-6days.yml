name: 6-Day RDP + Virtual Audio (VB-Cable)

on:
  workflow_dispatch:
    inputs:
      duration_days:
        description: 'Duration in days (1-6)'
        required: true
        default: '6'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
          - '4'
          - '5'
          - '6'

jobs:
  rdp-session:
    runs-on: windows-latest
    timeout-minutes: 8640

    steps:
      - name: Enable Remote Desktop
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-Port" dir=in action=allow protocol=TCP localport=3389
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "fAudioCapture" -Value 1 -Force
          Restart-Service -Name TermService -Force

      - name: Create RDP User
        run: |
          $chars = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*'
          $password = -join ((1..12) | ForEach-Object { $chars[(Get-Random -Maximum $chars.Length)] })
          $securePassword = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDPUser" -Password $securePassword -AccountNeverExpires
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDPUser"
          echo "RDP_PASSWORD=$password" >> $env:GITHUB_ENV

      - name: Set runneradmin password (optional)
        run: |
          $newPassword = "MyStrongPass123!"
          $securePassword = ConvertTo-SecureString $newPassword -AsPlainText -Force
          Set-LocalUser -Name "runneradmin" -Password $securePassword
          Write-Host "runneradmin password set to: $newPassword"

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i `"$installerPath`" /quiet /norestart" -Wait
          Remove-Item $installerPath -Force
          # Wait a bit for installation to settle
          Start-Sleep -Seconds 5
          # Verify installation
          $tailscaleExe = Get-ChildItem -Path "C:\Program Files\Tailscale" -Filter "tailscale.exe" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $tailscaleExe) {
            Write-Error "Tailscale installation failed: tailscale.exe not found."
            exit 1
          }
          echo "TAILSCALE_EXE=$($tailscaleExe.FullName)" >> $env:GITHUB_ENV
          Write-Host "Tailscale installed at: $($tailscaleExe.FullName)"

      - name: Connect Tailscale
        env:
          TS_AUTHKEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          if (-not $env:TS_AUTHKEY) {
            Write-Error "TAILSCALE_AUTH_KEY secret is missing or empty."
            exit 1
          }
          Write-Host "Auth key found (first 10 chars): $($env:TS_AUTHKEY.Substring(0,10))..."
          & "$env:TAILSCALE_EXE" up --authkey=$env:TS_AUTHKEY --hostname=gh-runner-$env:GITHUB_RUN_ID --accept-routes
          # Wait for IP
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 30) {
              $tsIP = & "$env:TAILSCALE_EXE" ip -4 2>$null
              if (-not $tsIP) {
                  Write-Host "Waiting for Tailscale IP... attempt $($retries+1)"
                  Start-Sleep -Seconds 5
                  $retries++
              } else {
                  break
              }
          }
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned after 2.5 minutes."
              & "$env:TAILSCALE_EXE" status
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
          Write-Host "Tailscale IP: $tsIP"

      - name: Restrict RDP Firewall to Tailscale Only
        run: |
          netsh advfirewall firewall delete rule name="RDP-Port"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389 remoteip=100.64.0.0/10
          Write-Host "RDP now accessible only via Tailscale IP."

      - name: Install VB-Cable Virtual Audio (Silent)
        run: |
          $url = "https://download.vb-audio.com/Download_CABLE/VBCABLE_Driver_Pack43.zip"
          $zipPath = "$env:TEMP\vbcable.zip"
          $extractPath = "$env:TEMP\vbcable"
          Invoke-WebRequest -Uri $url -OutFile $zipPath
          Expand-Archive -Path $zipPath -DestinationPath $extractPath -Force
          $installer = Get-ChildItem -Path $extractPath -Filter "VBCABLE_Setup_x64.exe" -Recurse | Select-Object -First 1
          if (-not $installer) {
            Write-Error "VB-Cable installer not found."
            exit 1
          }
          Write-Host "Installing VB-Cable silently..."
          Start-Process -FilePath $installer.FullName -ArgumentList "/VERYSILENT /SUPPRESSMSGBOXES /NORESTART" -Wait -NoNewWindow
          Remove-Item $zipPath -Force
          Remove-Item $extractPath -Recurse -Force
          Write-Host "VB-Cable installed."

      - name: Set VB-Cable as Default Audio Device (using nircmd)
        run: |
          Invoke-WebRequest -Uri "https://www.nirsoft.net/utils/nircmd.zip" -OutFile "$env:TEMP\nircmd.zip"
          Expand-Archive -Path "$env:TEMP\nircmd.zip" -DestinationPath "$env:TEMP\nircmd" -Force
          $nircmd = "$env:TEMP\nircmd\nircmd.exe"
          Start-Sleep -Seconds 10
          & $nircmd setdefaultsounddevice "CABLE Input" 1
          & $nircmd setdefaultsounddevice "CABLE Input" 2
          Write-Host "Default audio device set to CABLE Input (if present)."

      - name: Display Connection Info
        run: |
          Write-Host "`n========================================="
          Write-Host "     RDP CONNECTION INFO (Tailscale)"
          Write-Host "========================================="
          Write-Host ""
          Write-Host "🔒 Tailscale IP: $env:TAILSCALE_IP"
          Write-Host "👤 Username: RDPUser"
          Write-Host "🔑 Password: $env:RDP_PASSWORD"
          Write-Host ""
          Write-Host "🎧 Virtual Audio Cable installed. Use 'CABLE Input' as microphone/line-in."
          Write-Host "   It has been set as default playback device automatically."
          Write-Host ""
          Write-Host "Duration: ${{ inputs.duration_days }} days"
          Write-Host "Started: $(Get-Date)"
          Write-Host "Expires: $((Get-Date).AddDays(${{ inputs.duration_days }}))"
          Write-Host ""
          Write-Host "📱 iOS Connection:"
          Write-Host "1. Install Tailscale on your iPhone (App Store)"
          Write-Host "2. Log in with your Tailscale account"
          Write-Host "3. Install Microsoft Remote Desktop"
          Write-Host "4. Add PC with address: $env:TAILSCALE_IP"
          Write-Host "5. Username: RDPUser, Password: $env:RDP_PASSWORD"
          Write-Host "========================================="

      - name: Keep Session Alive
        run: |
          $days = ${{ inputs.duration_days }}
          $totalHours = $days * 24
          for ($i = 1; $i -le $totalHours; $i++) {
              $remaining = $totalHours - $i
              Write-Host "[$(Get-Date)] Hour $i of $totalHours - $remaining hours remaining"
              Start-Sleep -Seconds 3600
          }
